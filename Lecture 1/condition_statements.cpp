#include <iostream> // Why ?

using namespace std; // Why ?

int main() { // What is ?

	int wind = 0; // wind = 0;
	int friends = 12;

	if (wind == 1) {
		cout << "We Can't Play";
		cout << endl;
	} else {

		if (friends > 0) {
			cout << "We Can Play";
			cout << endl;

		}
	}

	return 0; // Why ?
}